// import React, { Suspense, lazy } from 'react'

// const loadComponent = () =>
//   <div>loading...</div>

// export default ((loader) => {
//   return React.lazy((loader) => import(`${loader}`))
// })
